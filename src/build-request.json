{
  "kind": "build_request",
  "title": "Add social profile links to the website header/footer",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-92",
      "text": "Add a reusable Social Links component that renders a row of social profile links (icon + accessible label) from a single centralized configuration, and only displays links that have a non-empty URL.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "recent-messages-6"
        ],
        "quotes": [
          "add social profiles link at website"
        ]
      },
      "acceptanceCriteria": [
        "A new reusable component exists (e.g., `frontend/src/components/common/SocialLinks.tsx`) that accepts a list of `{ label, href, icon }` items and renders only valid/defined `href` values.",
        "Each rendered social link uses an anchor tag with `target=\"_blank\"` and `rel=\"noopener noreferrer\"`.",
        "Each rendered social link has an accessible name via `aria-label` (English) and/or visible text for screen readers.",
        "If all configured URLs are empty/undefined, the component renders nothing (no empty container)."
      ]
    },
    {
      "id": "REQ-93",
      "text": "Add social profile links to the global site footer (`frontend/src/components/layout/Footer.tsx`) using the Social Links component, styled to match the existing footer typography and muted-foreground pattern.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "recent-messages-6"
        ],
        "quotes": [
          "add social profiles link at website"
        ]
      },
      "acceptanceCriteria": [
        "The footer displays a “Follow us” (English) label and a horizontal row of social icons/links when at least one social URL is configured.",
        "The social links visually match the existing footer style (consistent spacing, hover states, and muted text/icon color).",
        "No existing footer navigation links are removed or broken."
      ]
    },
    {
      "id": "REQ-94",
      "text": "Add social profile links to the global site header (`frontend/src/components/layout/Header.tsx`) in a space-efficient way that does not break the current desktop or mobile navigation layouts.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "recent-messages-6"
        ],
        "quotes": [
          "add social profiles link at website"
        ]
      },
      "acceptanceCriteria": [
        "On desktop, social icons/links appear in the header (e.g., near the theme toggle) without causing wrapping/overflow at common breakpoints.",
        "On mobile, social links are either included in the dropdown menu or otherwise accessible without crowding the header controls.",
        "All header social links meet the same accessibility and security requirements as the footer links (English aria-labels, new-tab behavior, `rel` attributes)."
      ]
    }
  ],
  "constraints": [
    "Do not modify any files under `frontend/src/components/ui` (immutable UI components).",
    "Keep user-facing text in English.",
    "No backend changes are required for this feature."
  ],
  "nonGoals": [
    "Building a full social feed integration (e.g., embedding Instagram/Twitter timelines).",
    "Adding third-party analytics/tracking pixels for social clicks.",
    "Adding Internet Identity or auth requirements for viewing social links."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  },
  "userProfileUpdate": {
    "goalsToAdd": [],
    "goalsToRemove": [],
    "preferencesToAdd": [],
    "preferencesToRemove": [],
    "miscToAdd": [],
    "miscToRemove": [],
    "fieldsToSet": {},
    "fieldsToDelete": []
  }
}